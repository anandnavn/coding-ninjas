#include<iostream>
#include <vector>
#include <set>

using namespace std;

vector<int> removeDuplicates(vector<int> v){

  	vector<int> :: iterator it ;
	  vector<int> v2;
	  set<int> s;
	   for(int i=0 ; i<v.size() ; i++){
	   	s.insert(v[i]);                                      //pushing vector values into set
	   }                                                    //set contains only unique elements , no repition of elements
	     set<int> :: iterator it2 ;
	     for(it2=s.begin() ; it2!=s.end() ; it2++){
	     	v2.push_back(*it2);                             //pushing set values into vector
	     }
	     
	   for(it=v2.begin() ; it<v2.end() ; it++){
	  	cout << *it << " " ;                                 //printing the new values of vector
	  }
	   
	return v2;

}

int main() {
	// your code goes here
	int size;
	cin >> size;
	vector<int> v;
	v.reserve(size);
	int input=0 ;
	   for(int i=0 ; i<size ; i++){
	   	cin >> input ;
	   	v.push_back(input);
	   }
	   
	 
	  cout << endl;
  vector<int> output = removeDuplicates(v);
    cout << endl;
    for(int i = 0; i < output.size(); i++){
        cout << output[i] << " ";
    }
	
	
	return 0;
}

